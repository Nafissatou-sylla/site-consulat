<form method="post" action="{{ path('app_passeport_disponible_delete', {'id': passeport_disponible.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ passeport_disponible.id) }}">
    <button class="btn">Delete</button>
</form>
