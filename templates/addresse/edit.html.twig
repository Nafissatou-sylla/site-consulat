{% extends 'home/base.html.twig' %}

{% block titre %} Les adresses {% endblock %}

{% block body %}
    <h1>Modifier  l'adresse</h1>

    {{ include('addresse/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('app_addresse_index') }}">back to list</a>

    {{ include('addresse/_delete_form.html.twig') }}
{% endblock %}
