{% extends './home/base.html.twig' %}

{% block body %}
    <h1></h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ user.id }}</td>
            </tr>
            <tr>
                <th>Prenom</th>
                <td>{{ user.prenom }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ user.nom }}</td>
            </tr>

            <tr>
                <th>Email</th>
                <td>{{ user.email }}</td>
            </tr>
            <tr>
                <th>Roles</th>
                <td>{{ user.roles ? user.roles|json_encode : '' }}</td>
            </tr>
            <tr>
                <th>Password</th>
                <td>{{ user.password }}</td>
            </tr>
            
        </tbody>
    </table>

    <a href="{{ path('app_user_index') }}">Liste des utilisateurs</a><br>

    <a href="{{ path('app_user_edit', {'id': user.id}) }}">Modifier</a><br>

    {{ include('user/_delete_form.html.twig') }}
{% endblock %}
